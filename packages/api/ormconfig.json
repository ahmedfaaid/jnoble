{
  "type": "postgres",
  "host": "localhost",
  "port": 5544,
  "username": "postgres",
  "password": "postgres",
  "database": "jnoble",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true,
  "logging": true,
  "migrations": ["src/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
